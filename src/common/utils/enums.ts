export enum ContentTypes {
    VIDEO = "video",
    // IMAGE = "image"
}

export enum Languages {
    HINDI = "hi",
    ENGLISH = "en",
    // IMAGE = "image"
}

export enum GetPresignedUrlEnum {
    put = 'put',
    get = 'get'
}

export enum Roles {
    student = 'student',
    employee = 'employee'
}